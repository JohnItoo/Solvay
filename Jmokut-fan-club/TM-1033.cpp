#include <bits/stdc++.h>

using namespace std;

int main() {
	int n; cin >> n;
	vector<vector<char > > grid(n, vector<char>(n));

	for (int i = 0; i < n; i++) {
		string x;
		cin >> x;
		for (int j = 0; j < n; j++) {
			grid[i][j] = string[j];
		}
	}

	return 0;
}